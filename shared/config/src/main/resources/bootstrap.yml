server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configures
  profiles:
    active: native
encrypt:
  key: foo